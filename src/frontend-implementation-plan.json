{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Romantic Valentine surprise page for Sunidhi with evasive No button and meme reveal",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Ensure the single-page prompt asks exactly “Will you be my Valentine Sunidhi🌸” and shows only “Yes” and “No” as interactive choices.",
      "acceptanceCriteria": [
        "On initial load, the only interactive choices visible to the user are buttons labeled “Yes” and “No”.",
        "The displayed prompt text includes “Sunidhi🌸” as specified."
      ],
      "file_operations": [
        {
          "path": "frontend/src/config/branding.ts",
          "operation": "modify",
          "description": "Update the prompt/title string to exactly “Will you be my Valentine Sunidhi🌸” (verbatim), keeping user-facing text centralized."
        },
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Ensure the prompt view renders only the required question and exactly two interactive buttons labeled “Yes” and “No” (no additional CTAs). Use shadcn-ui Button/Card for consistent tap targets and layout. Verify the component's usage instructions before implementing."
        },
        {
          "path": "frontend/index.html",
          "operation": "modify",
          "description": "Align the initial HTML title/metadata to the updated prompt wording so the first paint matches the exact required question text."
        }
      ]
    },
    {
      "id": "REQ-2",
      "summary": "Make the “No” button evasive and effectively unclickable across desktop hover and iPad Chrome touch/pointer interactions while staying smoothly within the visible prompt container.",
      "acceptanceCriteria": [
        "Hovering over “No” on desktop causes it to reposition away from the cursor within the prompt container.",
        "Attempting to press “No” on iPad Chrome (touch/pointer down) causes it to move away and prevents a click from registering.",
        "The “No” button never moves outside the visible bounds of the prompt container."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Refine the No-button reposition logic to (1) respond to desktop hover and iPad pointer/touch-down attempts, (2) keep motion smooth, and (3) clamp the computed coordinates so the button remains fully visible inside the prompt container at all times. Use shadcn-ui Button for the No control. Verify the component's usage instructions before implementing."
        }
      ]
    },
    {
      "id": "REQ-3",
      "summary": "On “Yes” click, replace the prompt with a success view showing the exact text “Good choice❤❤❤” and the meme image.",
      "acceptanceCriteria": [
        "Clicking “Yes” transitions to a new view/state where the prompt/buttons are no longer shown.",
        "The success view renders the exact message: “Good choice❤❤❤”.",
        "The success view displays the meme image below/alongside the message."
      ],
      "file_operations": [
        {
          "path": "frontend/src/config/branding.ts",
          "operation": "modify",
          "description": "Confirm/lock the success message string to exactly “Good choice❤❤❤” (verbatim) so the rendered text matches the requirement."
        },
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Implement/verify the success state that fully replaces the prompt/buttons and renders the exact success message plus the meme image. Use shadcn-ui Card to frame the success content. Verify the component's usage instructions before implementing."
        }
      ]
    },
    {
      "id": "REQ-4",
      "summary": "Apply a simple romantic pink/white theme with responsive, touch-friendly layout (iPad Chrome) and no horizontal overflow.",
      "acceptanceCriteria": [
        "Primary UI elements use a pink/white romantic palette.",
        "Buttons are comfortably tappable on iPad (no cramped spacing).",
        "No horizontal scrolling is required on iPad-sized viewports."
      ],
      "file_operations": [
        {
          "path": "frontend/index.css",
          "operation": "modify",
          "description": "Verify/adjust global theme tokens and base styles to maintain a pink/white romantic palette and prevent horizontal overflow issues in common tablet viewports."
        },
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Tune layout spacing/typography and button sizing to be readable and easily tappable on iPad, ensuring content remains within the viewport without horizontal scrolling. Use shadcn-ui Button/Card for consistent sizing. Verify the component's usage instructions before implementing."
        },
        {
          "path": "frontend/src/App.tsx",
          "operation": "modify",
          "description": "Ensure the page-level container enforces a responsive centered layout and guards against horizontal overflow across iPad-sized viewports."
        }
      ]
    },
    {
      "id": "REQ-5",
      "summary": "Keep the Valentine interaction fully frontend-only with no backend calls or dependency on backend state.",
      "acceptanceCriteria": [
        "The Valentine interaction works without any backend calls.",
        "The backend canister does not store or require any state for the Valentine flow."
      ],
      "file_operations": [
        {
          "path": "frontend/src/main.tsx",
          "operation": "modify",
          "description": "Remove/avoid wrapping the app in providers that initialize authentication/actor infrastructure for this single-page experience, keeping runtime strictly frontend-only."
        },
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Confirm the interaction logic is entirely local state (no imports/usages of backend actor hooks or network calls) and remains functional offline/without backend connectivity."
        }
      ]
    },
    {
      "id": "REQ-6",
      "summary": "Add the generated meme image as a static frontend asset and render it directly (no backend fetch).",
      "acceptanceCriteria": [
        "The meme image file exists under frontend/public/assets/generated/ with the exact required filename.",
        "The success view loads the meme image successfully in a production build."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/generated/valentine-good-choice-meme.dim_1200x800.png",
          "operation": "create",
          "description": "Add the generated static meme asset at the exact required path: frontend/public/assets/generated/valentine-good-choice-meme.dim_1200x800.png."
        },
        {
          "path": "frontend/src/components/ValentinePrompt.tsx",
          "operation": "modify",
          "description": "Wire the success view image to load from the static asset path frontend/public/assets/generated/valentine-good-choice-meme.dim_1200x800.png (no backend fetch). Use shadcn-ui Card to present the image cleanly. Verify the component's usage instructions before implementing."
        }
      ]
    }
  ]
}